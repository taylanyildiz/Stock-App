import 'package:flutter/material.dart';

class TableSide extends StatelessWidget {
  const TableSide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
